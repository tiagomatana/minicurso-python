
name: Bump version
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2.2.2
      - name: Config
        run: |
          pip install git-semver
          git config --global user.email "tiago.matana@gmail.com"
          git config --global user.name "Tiago Matana"
          git config --global github.token ${{ secrets.TOKEN }}
          git fetch --all
          git fetch --tags
      - name: Create Tag
        # You may pin to the exact commit or the version.
        # uses: odinr/action-npm-semver@37020c4b623853993ecaa7dd0f3d99b9ad5f2c2f
        run: |   
          VERSION=$(./git-version.sh)
          MSG=$(git log -1 --pretty=%B)
          git tag -a $VERSION -m '$MSG'
          git push --tags
#       - name: Github Action for curl
#         # You may pin to the exact commit or the version.
#         # uses: wei/curl@61d92b5169ea0425820dd13cf6fbad66b483e9f1
#         uses: wei/curl@v1.1.1
#         with:
#           args: |
#             -X POST -H "Accept: application/vnd.github.v3+json;" -H "Authorization: token $(git config --global github.token)" -d "$(./git-release.sh $VERSION $MSG)" https://api.github.com/repos/$repo_full_name/releases
     
        

